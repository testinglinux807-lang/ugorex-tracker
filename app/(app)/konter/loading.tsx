// Loading untuk navigasi di dalam segmen /konter (mis. daftar → detail
// toko). Boundary di level (app) tidak ke-trigger untuk perpindahan di
// dalam segmen yang sama, jadi perlu loading sendiri di sini.
export { default } from "@/app/(app)/loading";
