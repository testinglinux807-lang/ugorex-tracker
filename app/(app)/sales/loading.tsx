// Loading untuk navigasi di dalam segmen /sales (daftar → detail sales).
// Boundary di level (app) tidak ke-trigger untuk perpindahan di dalam
// segmen yang sama, jadi perlu loading sendiri di sini (pola sama /konter).
export { default } from "@/app/(app)/loading";
